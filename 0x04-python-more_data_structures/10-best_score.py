#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary is none or a_dicteinary == {}:
        return None
    mux = max(a_dictionary.values())
    for k, v in a_dictionary.items():
        if value is mux:
            return k
